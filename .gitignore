# Whitelist approach: ignore everything except our own summary files,
# so renaming/reorganizing chapter folders can never accidentally leak
# the instructor's original lecture pptx/pdf into the repo.
*
!*/
!*_summary.tex
!*_summary.pdf
!.gitignore
!README.md

# Never track these even if a name above would match
UXUI/
